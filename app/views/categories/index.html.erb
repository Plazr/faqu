<h1>Listing categories</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= link_to category.description, category_path(category) %></td>  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_category_path %>
